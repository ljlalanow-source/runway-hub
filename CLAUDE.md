# RUNWAY 허브 — 내 포트폴리오 사이트

이 폴더는 가족 AI 스터디 RUNWAY(2026년 9월~12월)에서 만든 결과물을 모아 보여주는 **나의 허브 사이트**다.
비핸스처럼 메인 페이지에 결과물이 카드로 나열되고, 카드마다 사용한 AI 도구가 태그로 붙는다.
답변과 산출물은 한국어로.
**굴림체는 절대 쓰지 않는다.** 새로 만드는 모든 페이지의 서체 스택에 한글 폴백(Pretendard → Noto Sans KR)을 넣는다. 모노 서체(IBM Plex Mono)는 한글이 없어 Windows에서 굴림으로 떨어지므로 반드시 `"IBM Plex Mono", "Pretendard", "Noto Sans KR", monospace` 순으로 쓴다.

## 폴더 구조

| 경로 | 용도 | 손대도 되나 |
|---|---|---|
| `index.html` | 허브 페이지. 다섯 명 공용 | **고치지 않는다.** 내용은 전부 `data.js`에서 |
| `data.js` | 내 소개 + 카드 목록. 유일하게 편집하는 파일 | 여기만 편집 |
| `thumbs/` | 카드 대표 사진 | 파일 추가 |
| `work/<카드id>/` | 결과물 원본. 웹페이지면 `index.html`, 그 외는 파일 그대로 | 폴더 추가 |
| `lectures/<id>/` | 강의 슬라이드 원본 (강의한 사람만). `data.js`의 `lectures`에 넣으면 Talks 탭이 생긴다 | 폴더 추가 |
| `tools/capture.ps1` `tools/capture.sh` | 결과물 페이지를 캡처해 대표 사진 만들기 | 실행만 |
| `fonts/Pretendard.woff2` | 사이트 서체 (한글 2350자 서브셋) | 손대지 않는다 |

## 카드 한 장 추가하는 절차

사용자가 "이번 결과물 카드 추가해줘"라고 하면 아래 순서대로 한다. 빠진 정보(제목, 도구)는 물어본다.

1. **카드 id 정하기** — `NN-영문슬러그` 형식. NN은 카드 순번 두 자리. 예: `02-december-me`
2. **결과물 저장** — `work/<id>/`에 넣는다. 웹페이지라면 `index.html`로 저장하고 아티팩트로 게시해 URL을 받는다. 이미지·PDF·영상 같은 파일은 그대로 넣는다.
3. **대표 사진 정하기** — 아래 둘 중 하나. 사용자가 사진을 주면 그것이 항상 우선한다.
   - 사용자가 사진을 준 경우: `thumbs/<id>.png`(또는 jpg)로 복사
   - 안 준 경우 + 결과물이 웹페이지: 캡처 스크립트로 자동 생성
     ```
     powershell -ExecutionPolicy Bypass -File tools/capture.ps1 -Target work/<id>/index.html -Out thumbs/<id>.png
     ```
     (맥이면 `bash tools/capture.sh work/<id>/index.html thumbs/<id>.png`)
   - 안 준 경우 + 결과물이 이미지: 그 이미지를 `thumbs/<id>.png`로 복사
   - 어느 것도 안 되면 `thumb`를 비워 둔다. 허브가 제목으로 색 타일을 자동으로 그린다.
4. **`data.js`의 `projects` 배열 맨 앞에 항목 추가** — 최신이 위로 온다. 형식은 `data.js` 안의 예시를 따른다.
5. **허브 재게시** — `index.html`을 아티팩트로 게시하되 `data.js`, `fonts/Pretendard.woff2`, `thumbs/` 안의 파일을 반드시 함께 올린다(supporting files). **처음 게시한 URL을 계속 유지한다.** 새 아티팩트를 만들지 않는다.
6. 게시된 링크를 사용자에게 알려준다.

## 카드 항목 형식

```js
{
  id: "02-december-me",              // 폴더·파일 이름과 같게
  title: "12월의 나",                  // 카드 제목
  category: "이미지 생성",              // 한 단어 분류 (선택)
  date: "2026.09",                    // 연.월
  tags: ["미드저니", "Claude"],        // 사용한 도구 이름만. 아래 표기 목록을 따른다
  url: "https://claude.ai/artifact/…", // 결과물 페이지 링크 (없으면 빼기)
  thumb: "thumbs/02-december-me.png", // 대표 사진 (없으면 빼기 → 자동 타일)
  badge: "NEW"                         // 카드 제목 앞 작은 표시 (선택)
}
```

### 태그 규칙

**태그는 지금 화면에 표시하지 않는다** (`profile.showTags: false`). 그래도 데이터에는 꼭 기록한다. 나중에 표시를 켜거나 다섯 명 것을 모을 때 쓴다.

태그는 **사용한 도구의 이름만** 쓴다. 분류나 기법(자동화, 리서치 같은 말)은 태그로 쓰지 않는다.
다섯 명이 나중에 한 곳에서 모아 볼 수 있도록 표기를 통일한다.

- **결과물 자체를 만드는 데 쓴 도구만 태그한다.** 허브에 올리려고 페이지로 감싼 작업(Claude Code로 index.html 만들기, 게시하기)은 태그하지 않는다. 그걸 붙이면 모든 카드에 Claude Code가 붙어 필터가 무의미해진다.
- **`Claude`와 `Claude Code`는 다른 도구다.** 채팅창에서 대화·글쓰기·리서치·프롬프트 작성을 했으면 `Claude`. 내 노트북의 파일을 직접 만들거나 고쳤으면(사이트·도구·자동화) `Claude Code`. 둘 다 했으면 둘 다.
  - 예: "12월의 나" 이미지 카드 → `미드저니`, `Claude` (이미지 페이지를 Claude Code로 감쌌어도 안 붙임)
  - 예: 6회차 견적 관리 도구 → `Claude Code` (결과물이 곧 코드)

`Claude` · `Claude Code` · `ChatGPT` · `Gemini` · `미드저니` · `피그마` · `힉스필드` · `노션` · `Whisper` · `ffmpeg` · `Runway` · `일레븐랩스` · `캔바` · `프리미어` · `블렌더`

목록에 없는 도구는 공식 표기(한글 표기가 흔하면 한글)로 추가하고, 이 목록에도 한 줄 보탠다.

### 결과물 페이지에 담는 것 (선택)

허브에는 카드만 있다. 카드를 누르면 결과물 페이지가 **새 창**으로 열린다.
결과물이 웹페이지라면 그 안에 "문제 / 과정(AI를 어떻게 다뤘나) / 나의 자리 / 결과" 4단을 써 두면 나중에 비핸스·노트폴리오에 그대로 옮길 수 있다. 예시: `work/04-reels-pipeline/index.html`.

## 강의 슬라이드 (선택) — 스터디에서 직접 강의한 사람만

허브 상단 **Talks** 탭과 목록은 `data.js`의 `lectures` 배열에 항목이 있을 때만 나타난다. 비어 있으면 아무것도 안 보인다. 카드가 아니라 **목록**이라 대표 사진이 필요 없다.

1. 원본은 `lectures/<id>/index.html`에 둔다. id는 `NN-영문슬러그`. 예: `01-ai-agent`
2. 아티팩트로 게시한다(`fonts/Pretendard.woff2`를 supporting file로 같이). URL을 받는다.
3. `data.js`의 `lectures` 배열 **맨 앞**에 항목 추가 (최신이 위). 형식:
   ```js
   { id: "01-ai-agent", title: "AI 에이전트 이해", desc: "한 줄 요약", session: "1회차 시동", date: "2026.09", url: "https://claude.ai/artifact/…" }
   ```
4. 허브 재게시 (위 카드 추가 절차 5번과 같음).

강의 페이지는 **넘겨 보는 슬라이드 덱**(16:9 무대 1280×720을 화면에 맞춰 축소, ←→/Space/숫자키 이동, F 또는 버튼으로 전체화면, 터치 스와이프)으로 만든다. 서체는 허브와 같고(Pretendard + IBM Plex Mono) 색은 **다크 고정**: 검정 바탕 + 레몬(#F9FA82) 포인트만(2026-09-23 LJ: 밝은 화면은 눈이 아프다). 오른쪽에 슬라이드 번호 세로 내비. 예시: `lectures/01-ai-agent/index.html`. 같은 내용의 읽기용 노트는 `notes.html`로 옆에 둔다(게시 안 함). 슬라이드의 사진(영화 스틸 등)은 저작권 문제로 싣지 않고 글로 푼다.

## 첫 번째 프로젝트 — "3년 뒤의 나" 프로필 사이트 (1회차)

첫 카드는 **3년 뒤의 내가 클라이언트에게 보내는 원페이지 프로필**이다. 틀은 `work/01-profile/`에 이미 들어 있다.

- `work/01-profile/INTERVIEW.md` — 인터뷰 대본. 사용자가 "INTERVIEW.md대로 인터뷰해줘"라고 하면 그대로 따른다.
- `work/01-profile/profile.js` — 인터뷰 답이 들어가는 내용 파일. **이 파일만 채운다.** `index.html`은 틀이라 손대지 않는다. 예외 하나: 키컬러 — `:root`의 `--accent` · `--accent-ink` · `--accent-text` 세 값을 INTERVIEW.md 0단계 표대로 바꿔 허브와 색을 맞춘다.
- 흐름: 허브 설정 3개(이니셜·키컬러·2026 엔딩목표 — 사전 인터뷰 웹사이트 결과를 붙여넣기) → 기본 정보(나중에 입력 선택 가능) → 세 질문 → `profile.js` 채우기 → 확인 → `index.html`+`profile.js` 아티팩트 게시 → 허브 `data.js` profile 채우기 → 카드 `01-profile` 추가 → 허브 게시.
- 디자인은 기본 스타일이 디폴트. 바꾸고 싶으면 `index.html` 상단 `:root` 색과 서체만. 5개 영역 구조는 유지.
- 프로필 카드 썸네일은 INTERVIEW.md E의 **고정 앵글 캡처 명령**을 그대로 쓴다 (히어로만 크게, 3:2).

### 허브 `data.js`의 `profile` — 인터뷰에서 정하는 것은 세 가지뿐

| 항목 | 어디서 | 값 |
|---|---|---|
| `name` | INTERVIEW.md 0-1 | 이니셜. 예: "HS" |
| `accent` | INTERVIEW.md 0-2 | 키컬러 hex 하나. 배지·엔딩목표 막대·강조색. 진한 글자색과 연한 배경은 페이지가 자동으로 만든다. 프로필 `index.html` `:root`에도 같은 색(세 값)을 넣는다 |
| `goal`, `goalNote` | INTERVIEW.md 0-3 | 2026 엔딩목표 한 줄 + 단계 한 줄(선택) |
| `bio` | A3 답에서 | 내 정보 한 줄. 예: "커뮤니케이션 디자이너 엘제이." |

`headline`("AI로 날개를 달고 / 멋진 것들을 만들어봐야지"), `intro`, `brand`("AI Runway Log"), `eyebrow`는 **다섯 명 공통 문구**라 바꾸지 않는다.
`siteTitle`은 "이니셜 · AI Runway Log"로. `showTags`는 태그 표시 스위치(기본 false).
**들은 것만** 쓴다. 듣지 않은 경력·수상·수치는 지어내지 않는다. 다 쓴 뒤 사용자에게 틀린 곳이 있는지 확인받는다.

## 안전 규칙

- 이 사이트는 공개된다. 전화번호·주소·재직 중인 회사의 내부 자료·고객 정보는 올리지 않는다.
- 결과물에 회사 자료나 고객 정보가 있으면 익명화하거나 요약한 뒤에 넣는다.
- AI가 출처나 사실을 댔다고 그대로 믿지 않는다. 확인한 것만 남긴다.
